# Procedural Kernel — Release Notes

## 0.1.3.1

- Atualização do `UBU Version Governor` para `0.1.1`.
- Contrato `version-chain.json` atualizado com `push.forceWithLease`.
- Governor passa a ser versionado no repositório para evitar dependência de artefato local.

## 0.1.3

- Sincronização UBU ISO e patch de arquivos faltantes.
