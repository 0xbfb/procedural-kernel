# Aplicação do patch 0.1.3.1

Este overlay atualiza o `procedural-kernel` de `0.1.3` para `0.1.3.1` com foco exclusivo no `UBU Version Governor`.

## Aplicação

Extraia este zip na raiz do repositório.

## Observação importante

A partir desta versão, `tools/ubu-version-governor` deixa de ser ignorado e passa a ser versionado no repositório para que o fechamento de releases seja reproduzível.
